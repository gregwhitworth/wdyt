<script>
  //let tl = gsap.timeline({ repeat: -1 });
  /*tl.to("rect:nth-child(3n)", {duration: 5, x: 50, ease: "power2.out"})
    .to("rect:nth-child(3n)", {duration: 5, x: 0, ease: "power2.in"})
    .to("rect:nth-child(2n)", {duration: 5, x: 50, ease: "power2.out"})
    .to("rect:nth-child(2n)", {duration: 5, x: 0, ease: "power2.in"});*/

  gsap.to("rect", 7, {
    scale: 1,
    x: 50,
    yoyo: true,
    repeat: -1,
    ease: "power1.inOut",
    delay:0,
    stagger: {
      amount: 1.5,
      grid: "auto",
      from: "center"
    }
  });

</script>